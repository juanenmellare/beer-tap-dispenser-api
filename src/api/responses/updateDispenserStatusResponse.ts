class UpdateDispenseStatusResponse {
  public readonly message!: string;

  constructor() {
    this.message = 'Status of the tap changed correctly';
  }
}

export default UpdateDispenseStatusResponse;
